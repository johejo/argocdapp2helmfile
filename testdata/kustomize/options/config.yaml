apiVersion: argocdapp2helmfile/v1alpha1
kind: Config
sources:
  - repoURL: ssh://git@git.example.com/platform/manifests.git
    targetRevision: 0123456789abcdef
    root: '{{ requiredEnv "MANIFESTS_ROOT" }}'
