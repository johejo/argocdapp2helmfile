apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: unknown-field
spec:
  generators:
    - merge:
        mergeKeys: [name]
        generators:
          - list:
              elements:
                - name: first
          - list:
              elements:
                - name: second
        extra: rejected
  template:
    metadata:
      name: '{{name}}'
    spec: {}
